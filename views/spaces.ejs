<!DOCTYPE html>
<html>
  <head>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>

  <body>
    <h1><%= title %></h1>
    <form method="get" action="/confirm">
		<ul><% addresses.forEach(function(address) { %>
			<li> <%= address %>
       <input type="radio" id="a" value="a"></li>
			<% }) %>
		</ul>

    <button type=submit>Request</button>
    </form>
  </body>
</html>
